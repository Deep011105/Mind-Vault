package com.lifevault.config;

/**
 * CORS configuration has been moved to {@link AppConfig#corsConfigurationSource()}.
 * A {@code CorsConfigurationSource} bean is required for Spring Security 6.x CORS
 * integration; the old {@code WebMvcConfigurer}-based approach is superseded.
 *
 * This file is intentionally left empty and can be deleted if desired.
 */
public class CorsConfig {
}
